title = "Shine Backend secret scanning"

[extend]
useDefault = true

# Do not add broad path or regex allowlists here. A false positive must use an
# exact fingerprint in .gitleaksignore and follow docs/security-ci.md.
